{% extends 'layout.twig' %}

{% block title %}
    {% trans %}title.protect{% endtrans %}
{% endblock %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('css/form.user.css') }}" />
{% endblock %}

{% block content %}
    <form action="{{ url('AlbumCrypt', { 'albumPath': albumPath } ) }}" method="POST">
        {% include 'errors.twig' %}

        <p class="info">
            {% trans %}form.protect.intro{% endtrans %}
        </p>

        <p>
            <label for="password">
                {% trans %}form.protect.password{% endtrans %}
            </label>
            <input type="password" id="password" name="password" autofocus="autofocus" />
        </p>

        <p>
            <input type="submit" value="{% trans %}form.protect.submit{% endtrans %}" />
        </p>

        <p>
            <a
                class="button"
                href="{{
                    url(
                        'AlbumDisplay',
                        {
                            'albumPath': albumPath,
                            'page'     : _session.get('lastPage',      1 ),
                            'nbPerPage': _session.get('lastNbPerPage', 50),
                        }
                    )
                }}"
            >
                {% trans %}form.protect.cancel{% endtrans %}
            </a>
        </p>
    </form>
{% endblock %}